Add a notes editing feature to the project context menu (ProjectContextMenu.tsx).

Requirements:
1. Add a "Notes" menu item that opens an inline textarea
2. Use the existing `setProjectNotes` API from src/lib/tauri.ts
3. The Project type already has a `notes?: string` field in src/lib/types.ts
4. Show a small pencil icon next to projects that have notes
5. Keep it simple - just add the menu item and a small inline editor

After changes, verify with: npx tsc --noEmit
